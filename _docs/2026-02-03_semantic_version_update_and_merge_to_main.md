# セマンティックバージョニングの更新とmainブランチへのマージ

## 概要

`release/v0.28.0`
ブランチにおいて、セマンティックバージョニングの更新を行い、`main`
ブランチにマージしました。

## 詳細

1.  `package.json` の `version` を `0.28.1` から `0.28.2` に更新しました。
2.  `package.json` の `config.sandboxImageUri` のバージョンを `0.28.1` から
    `0.28.2` に更新しました。
3.  上記の変更をコミットしました。(commit: `b78e277fb`)
4.  `release/v0.28.0` ブランチを `main` ブランチにマージしました。
