/**
 * @license
 * Copyright 2025 Google LLC
 * SPDX-License-Identifier: Apache-2.0
 */
import type { Subagent } from '../config/subagents.js';
import { GeminiClient } from './geminiClient.js';
/**
 * サブエージェントタスク定義
 */
export interface SubagentTask {
    id: string;
    task: string;
    context?: string;
    priority: 'low' | 'medium' | 'high' | 'urgent';
    timeout?: number;
    dependencies?: string[];
    metadata?: Record<string, unknown>;
}
/**
 * サブエージェント実行結果
 */
export interface SubagentResult {
    subagentId: string;
    result: string;
    status: 'success' | 'failed' | 'partial';
    executionTime: number;
    tokensUsed?: number;
    qualityScore?: number;
    confidenceLevel?: number;
    error?: string;
    metadata?: Record<string, unknown>;
}
/**
 * サブエージェント実行オプション
 */
export interface SubagentExecutorOptions {
    geminiClient?: GeminiClient;
    maxConcurrent?: number;
    timeout?: number;
    onProgress?: (message: string, type: 'info' | 'success' | 'error' | 'progress') => void;
}
/**
 * サブエージェント実行器
 */
/**
 * サブエージェント定義のYAML設定を読み込むためのインターフェース
 */
export interface SubagentDefinition {
    name: string;
    description: string;
    model?: string;
    color?: string;
    specialty: string;
    triggers?: string[];
    capabilities?: string[];
    config?: Record<string, unknown>;
}
/**
 * サブエージェントの自動登録システム
 */
export declare class SubagentRegistry {
    private static instance;
    private subagents;
    static getInstance(): SubagentRegistry;
    register(definition: SubagentDefinition): void;
    getSubagent(name: string): SubagentDefinition | undefined;
    getAllSubagents(): SubagentDefinition[];
    getSubagentsBySpecialty(specialty: string): SubagentDefinition[];
}
export declare class SubagentExecutor {
    private geminiClient;
    private maxConcurrent;
    private timeout;
    private onProgress?;
    private colorManager;
    constructor(options?: SubagentExecutorOptions);
    /**
     * 進捗メッセージを送信
     */
    private sendProgress;
    /**
     * 単一サブエージェントタスク実行
     */
    executeTask(subagent: Subagent, task: SubagentTask): Promise<SubagentResult>;
    /**
     * サブエージェント並列実行
     */
    executeParallel(subagents: Subagent[], task: SubagentTask): Promise<SubagentResult[]>;
    /**
     * タスクプロンプト生成
     */
    private generateTaskPrompt;
    /**
     * 配列を指定サイズのチャンクに分割
     */
    private chunkArray;
    /**
     * 実行状況の詳細表示
     */
    displayExecutionStatus(subagents: Subagent[], activeCount: number, completedCount: number): void;
}
