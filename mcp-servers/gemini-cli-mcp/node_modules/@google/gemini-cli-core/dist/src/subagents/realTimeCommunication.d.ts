/**
 * @license
 * Copyright 2025 Google LLC
 * SPDX-License-Identifier: Apache-2.0
 */
import { EventEmitter } from 'events';
import { RealTimeMessage, RealTimeMessageType, RealTimeSessionConfig, RealTimeConnectionState, RealTimeCommunicationStats } from './types.js';
/**
 * WebSocketベースのリアルタイム通信システム
 * メインエージェントとサブエージェント間の協調的リアルタイム通信を管理
 */
export declare class RealTimeCommunicationSystem extends EventEmitter {
    private config;
    private connections;
    private connectionStates;
    private messageQueue;
    private stats;
    private heartbeatInterval;
    private checkpointInterval;
    private retryCounts;
    private messageHandlers;
    constructor(config: RealTimeSessionConfig);
    /**
     * メッセージハンドラーの初期化
     */
    private initializeMessageHandlers;
    /**
     * WebSocket接続を確立
     */
    connect(agentId: string, url: string): Promise<boolean>;
    /**
     * メッセージ送信
     */
    sendMessage(message: RealTimeMessage): Promise<boolean>;
    /**
     * 受信メッセージの処理
     */
    private handleIncomingMessage;
    /**
     * メッセージハンドラーの実行
     */
    private executeMessageHandlers;
    /**
     * メッセージハンドラーの登録
     */
    onMessage(type: RealTimeMessageType, handler: (message: RealTimeMessage) => void): void;
    /**
     * タスク割り当てメッセージの送信
     */
    assignTask(taskId: string, task: string, subagentId: string, context?: string, priority?: 'low' | 'medium' | 'high' | 'urgent'): Promise<boolean>;
    /**
     * タスク進捗メッセージの送信
     */
    reportProgress(taskId: string, progress: number, status: 'started' | 'in_progress' | 'paused' | 'resumed' | 'completed' | 'failed', agentId: string, currentStep?: string): Promise<boolean>;
    /**
     * タスク完了メッセージの送信
     */
    reportCompletion(taskId: string, result: unknown, executionTime: number, tokensUsed: number, qualityScore: number, confidenceLevel: number, agentId: string): Promise<boolean>;
    /**
     * 協調要求メッセージの送信
     */
    requestCoordination(requestType: 'help' | 'review' | 'integration' | 'validation' | 'optimization', taskId: string, description: string, urgency: 'low' | 'medium' | 'high' | 'urgent', senderId: string, targetId?: string): Promise<boolean>;
    /**
     * メインエージェント指示メッセージの送信
     */
    sendDirective(directiveType: 'pause' | 'resume' | 'redirect' | 'optimize' | 'validate' | 'integrate', instruction: string, targetSubagentId?: string, taskId?: string, priority?: 'low' | 'medium' | 'high' | 'urgent'): Promise<boolean>;
    /**
     * パフォーマンスメトリクスの送信
     */
    sendPerformanceMetrics(agentId: string, cpuUsage: number, memoryUsage: number, responseTime: number, throughput: number, errorRate: number, activeTasks: number): Promise<boolean>;
    /**
     * セッション制御メッセージの送信
     */
    controlSession(action: 'start' | 'pause' | 'resume' | 'stop' | 'restart' | 'checkpoint' | 'restore', reason?: string): Promise<boolean>;
    /**
     * ハートビートの開始
     */
    private startHeartbeat;
    /**
     * ハートビートメッセージの送信
     */
    private sendHeartbeat;
    /**
     * チェックポイントの開始
     */
    private startCheckpointing;
    /**
     * チェックポイントの保存
     */
    private saveCheckpoint;
    /**
     * メッセージのキューイング
     */
    private queueMessage;
    /**
     * キューされたメッセージの送信
     */
    private sendQueuedMessages;
    /**
     * メッセージの検証
     */
    private validateMessage;
    /**
     * エラー率の更新
     */
    private updateErrorRate;
    /**
     * メッセージIDの生成
     */
    private generateMessageId;
    /**
     * 接続状態の取得
     */
    getConnectionState(agentId: string): RealTimeConnectionState | undefined;
    /**
     * 全接続状態の取得
     */
    getAllConnectionStates(): RealTimeConnectionState[];
    /**
     * 通信統計の取得
     */
    getStats(): RealTimeCommunicationStats;
    /**
     * システムの停止
     */
    shutdown(): Promise<void>;
}
