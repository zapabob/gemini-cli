/**
 * @license
 * Copyright 2025 Google LLC
 * SPDX-License-Identifier: Apache-2.0
 */
import { Subagent } from '../config/subagents.js';
export interface GeminiRequest {
    prompt: string;
    context?: string;
    model?: string;
    temperature?: number;
    maxTokens?: number;
}
export interface GeminiResponse {
    text: string;
    tokensUsed: number;
    model: string;
    finishReason: string;
}
export interface GeminiError {
    code: string;
    message: string;
    details?: unknown;
}
/**
 * Gemini APIクライアント
 */
export declare class GeminiClient {
    private apiKey;
    private baseUrl;
    private defaultModel;
    private defaultTemperature;
    private defaultMaxTokens;
    constructor(options: {
        apiKey: string;
        baseUrl?: string;
        defaultModel?: string;
        defaultTemperature?: number;
        defaultMaxTokens?: number;
    });
    /**
     * サブエージェント用のプロンプトを生成
     */
    private generateSubagentPrompt;
    /**
     * 専門分野の説明を取得
     */
    private getSpecialtyDescription;
    /**
     * Gemini APIにリクエストを送信
     */
    generateText(request: GeminiRequest): Promise<GeminiResponse>;
    /**
     * サブエージェントのタスクを実行
     */
    executeSubagentTask(subagent: Subagent, task: string, context?: string, options?: Partial<GeminiRequest>): Promise<GeminiResponse>;
    /**
     * 複数のサブエージェントで並列実行
     */
    executeParallelTasks(subagents: Subagent[], task: string, context?: string, options?: Partial<GeminiRequest>): Promise<Array<{
        subagent: Subagent;
        response: GeminiResponse;
    }>>;
    /**
     * APIキーの有効性を確認
     */
    validateApiKey(): Promise<boolean>;
}
