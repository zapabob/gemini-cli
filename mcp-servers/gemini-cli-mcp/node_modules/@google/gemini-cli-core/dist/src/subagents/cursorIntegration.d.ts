/**
 * @license
 * Copyright 2025 Google LLC
 * SPDX-License-Identifier: Apache-2.0
 */
import { SubagentSpecialty } from '../config/subagents.js';
/**
 * Cursor IDEとの連携設定
 */
export interface CursorIntegrationConfig {
    enableRealTimeSync: boolean;
    enableAutoCodeReview: boolean;
    enableParallelExecution: boolean;
    enableCommandPalette: boolean;
    enableFileWatcher: boolean;
    enableLiveCollaboration: boolean;
    maxConcurrentTasks: number;
    syncInterval: number;
    autoSaveInterval: number;
}
/**
 * Cursorファイル変更イベント
 */
export interface CursorFileChangeEvent {
    filePath: string;
    changeType: 'created' | 'modified' | 'deleted' | 'renamed';
    content?: string;
    timestamp: Date;
    userId?: string;
}
/**
 * Cursorコマンド実行結果
 */
export interface CursorCommandResult {
    success: boolean;
    output: string;
    error?: string;
    executionTime: number;
    subagentResults?: unknown[];
}
/**
 * Cursor並列実行タスク
 */
export interface CursorParallelTask {
    id: string;
    task: string;
    filePath?: string;
    specialty: SubagentSpecialty;
    priority: 'high' | 'medium' | 'low';
    status: 'pending' | 'running' | 'completed' | 'failed';
    result?: unknown;
    startTime?: Date;
    endTime?: Date;
}
/**
 * Cursor IDE連携マネージャー
 * Cursorとのリアルタイム連携、並列実行、自律的サブエージェント呼び出しを管理
 */
export declare class CursorIntegrationManager {
    private config;
    private mainAgent;
    private colorManager;
    private checkpointManager;
    private geminiClient;
    private activeTasks;
    private fileWatchers;
    private syncInterval;
    private autoSaveInterval;
    constructor(config: CursorIntegrationConfig);
    /**
     * Cursor連携の初期化
     */
    private initializeCursorIntegration;
    /**
     * リアルタイム同期の開始
     */
    private startRealTimeSync;
    /**
     * Cursorとの同期
     */
    private syncWithCursor;
    /**
     * 現在のCursorファイルを取得
     */
    private getCurrentCursorFiles;
    /**
     * ファイルの変更を検出
     */
    private hasFileChanged;
    /**
     * ファイル変更の処理
     */
    private handleFileChange;
    /**
     * ファイル変更イベントの処理
     */
    processFileChangeEvent(event: CursorFileChangeEvent): Promise<void>;
    /**
     * 自動コードレビューの実行
     */
    private triggerAutoCodeReview;
    /**
     * コードレビュー提案の適用
     */
    private applyCodeReviewSuggestions;
    /**
     * 協調者への通知
     */
    private notifyCollaborators;
    /**
     * 並列タスクの実行
     */
    executeParallelTask(task: string, filePath?: string, specialty?: SubagentSpecialty): Promise<string>;
    /**
     * 並列タスクのキュー追加
     */
    private queueParallelTask;
    /**
     * コマンドパレットの登録
     */
    private registerCommandPalette;
    /**
     * コマンドの実行
     */
    executeCommand(commandId: string, context?: unknown): Promise<CursorCommandResult>;
    /**
     * コードレビューコマンドの実行
     */
    private executeCodeReviewCommand;
    /**
     * デバッグコマンドの実行
     */
    private executeDebugCommand;
    /**
     * 最適化コマンドの実行
     */
    private executeOptimizeCommand;
    /**
     * セキュリティコマンドの実行
     */
    private executeSecurityCommand;
    /**
     * 並列コマンドの実行
     */
    private executeParallelCommand;
    /**
     * ファイル監視の開始
     */
    private startFileWatching;
    /**
     * 自動保存の開始
     */
    private startAutoSave;
    /**
     * アクティブタスクの取得
     */
    getActiveTasks(): CursorParallelTask[];
    /**
     * タスクの状態取得
     */
    getTaskStatus(taskId: string): CursorParallelTask | undefined;
    /**
     * 設定の更新
     */
    updateConfig(updates: Partial<CursorIntegrationConfig>): void;
    /**
     * クリーンアップ
     */
    cleanup(): Promise<void>;
    /**
     * タスクIDの生成
     */
    private generateTaskId;
}
