/**
 * @license
 * Copyright 2025 Google LLC
 * SPDX-License-Identifier: Apache-2.0
 */
import { Subagent, SubagentSpecialty } from '../config/subagents.js';
import { GeminiClient } from './geminiClient.js';
import { CollaborativeTaskOptions, CollaborativeTaskResult } from './types.js';
/**
 * AI自律的オーケストレーター設定
 */
export interface AIAutonomousOrchestratorConfig {
    geminiClient: GeminiClient;
    maxSubagents: number;
    enableAutoAnalysis: boolean;
    enableRealTimeCoordination: boolean;
    decisionThreshold: number;
    timeout: number;
    enableCheckpointing: boolean;
    checkpointInterval: number;
    enableAutoSubagentSelection: boolean;
    enableDynamicTaskBreakdown: boolean;
    enableIntelligentCoordination: boolean;
}
/**
 * AI自律的タスク分析結果
 */
export interface AIAutonomousTaskAnalysis {
    originalTask: string;
    complexity: number;
    requiredSpecialties: SubagentSpecialty[];
    estimatedSubagents: number;
    parallelizable: boolean;
    dependencies: string[];
    riskLevel: 'low' | 'medium' | 'high';
    estimatedTime: number;
    subtasks: string[];
    coordinationStrategy: 'parallel' | 'sequential' | 'hybrid' | 'adaptive';
    priority: 'low' | 'medium' | 'high';
    autoResearchRequired: boolean;
    researchQuery?: string;
}
/**
 * AI自律的サブエージェント選択結果
 */
export interface AIAutonomousSubagentSelection {
    selectedSubagents: Subagent[];
    assignmentStrategy: 'parallel' | 'sequential' | 'hybrid' | 'adaptive';
    taskBreakdown: Map<string, string>;
    coordinationPlan: string;
    executionOrder: string[];
    parallelGroups: string[][];
    fallbackPlan: string;
}
/**
 * AI自律的オーケストレーター
 * AI側でサブエージェントを自律的に呼び出して作業を分担させる
 */
export declare class AIAutonomousOrchestrator {
    private config;
    private executor;
    private geminiClient;
    private taskHistory;
    constructor(config: AIAutonomousOrchestratorConfig);
    /**
     * AI側からの自律的タスク実行
     */
    executeAIAutonomousTask(task: string, context?: string, options?: CollaborativeTaskOptions): Promise<CollaborativeTaskResult>;
    /**
     * AI自律的タスク分析
     */
    private analyzeAITask;
    /**
     * AI自律的サブエージェント選択
     */
    private selectAISubagents;
    /**
     * AI自律的タスク分割と実行
     */
    private executeWithAISubagents;
    /**
     * 適応的実行戦略
     */
    private executeAdaptiveStrategy;
    /**
     * ハイブリッド実行戦略
     */
    private executeHybridStrategy;
    /**
     * 適応的戦略決定
     */
    private determineAdaptiveStrategy;
    /**
     * AI自律的結果統合
     */
    private integrateAIResults;
    /**
     * AIメトリクス計算
     */
    private calculateAIMetrics;
    /**
     * 協調効率計算
     */
    private calculateCoordinationEfficiency;
    /**
     * リソース利用率計算
     */
    private calculateResourceUtilization;
    /**
     * 利用可能なサブエージェント取得
     */
    private getAvailableSubagents;
    /**
     * フォールバックAI分析
     */
    private fallbackAIAnalysis;
    /**
     * フォールバックAIサブエージェント選択
     */
    private fallbackAISubagentSelection;
    /**
     * 進捗ハンドラー
     */
    private handleProgress;
    /**
     * タスクID生成
     */
    private generateTaskId;
    /**
     * AI実装ログ保存
     */
    private saveAIImplementationLog;
    /**
     * TaskAnalysis変換
     */
    private convertToTaskAnalysis;
}
