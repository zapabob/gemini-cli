/**
 * @license
 * Copyright 2025 Google LLC
 * SPDX-License-Identifier: Apache-2.0
 */
import { GeminiClient } from './geminiClient.js';
import { CollaborativeTaskResult, CollaborativeTaskOptions, CollaborationSession } from './types.js';
import { Config } from '../config/config.js';
/**
 * メインエージェントインターフェース設定
 */
export interface MainAgentInterfaceConfig {
    geminiClient: GeminiClient;
    config: Config;
    enableAutonomousMode: boolean;
    enableSupervisorMode: boolean;
    enableNaturalLanguageProcessing: boolean;
    maxConcurrentSubagents: number;
    autoAnalysisThreshold: number;
    decisionTimeout: number;
    enableRealTimeCoordination: boolean;
    enableCheckpointing: boolean;
    researchOutputPath: string;
}
export type TaskExecutionMode = 'autonomous' | 'supervisor' | 'manual' | 'auto' | 'natural_language';
/**
 * メインエージェントインターフェース
 * メインエージェントが自律的にサブエージェントを呼び出すための統合インターフェース
 */
export declare class MainAgentInterface {
    private config;
    private orchestrator;
    private supervisor;
    private naturalLanguageProcessor?;
    private geminiClient;
    private taskHistory;
    private activeSessions;
    constructor(config: MainAgentInterfaceConfig);
    /**
     * メインエージェントからのタスク実行
     * 自律的にサブエージェントの使用を決定
     */
    executeTask(task: string, context?: string, mode?: TaskExecutionMode, options?: CollaborativeTaskOptions): Promise<CollaborativeTaskResult>;
    /**
     * 実行モードの自動決定
     */
    private determineExecutionMode;
    /**
     * 自律モードでの実行
     */
    private executeAutonomousMode;
    /**
     * 監督者モードでの実行
     */
    private executeSupervisorMode;
    /**
     * 手動モードでの実行
     */
    private executeManualMode;
    /**
     * 自然言語プロンプト実行モード
     */
    private executeNaturalLanguageMode;
    /**
     * リアルタイム協調セッションの開始
     */
    startRealTimeCollaboration(task: string, _context?: string, _options?: CollaborativeTaskOptions): Promise<string>;
    /**
     * リアルタイム協調セッションへの参加
     */
    joinCollaborationSession(sessionId: string, subagentId: string): Promise<boolean>;
    /**
     * リアルタイム協調セッションの終了
     */
    endCollaborationSession(sessionId: string): Promise<CollaborativeTaskResult>;
    /**
     * タスク履歴の取得
     */
    getTaskHistory(): Map<string, CollaborativeTaskResult>;
    /**
     * 特定のタスク結果の取得
     */
    getTaskResult(taskId: string): CollaborativeTaskResult | undefined;
    /**
     * アクティブセッションの取得
     */
    getActiveSessions(): Map<string, CollaborationSession>;
    /**
     * パフォーマンス統計の取得
     */
    getPerformanceStats(): {
        totalTasks: number;
        successfulTasks: number;
        averageExecutionTime: number;
        totalSubagentsUsed: number;
        mostUsedMode: TaskExecutionMode;
    };
    /**
     * 設定の更新
     */
    updateConfig(updates: Partial<MainAgentInterfaceConfig>): void;
    /**
     * タスクID生成
     */
    private generateTaskId;
    /**
     * 電源断保護機能: チェックポイント保存
     */
    saveCheckpoint(): Promise<void>;
    /**
     * 電源断保護機能: チェックポイント復元
     */
    restoreCheckpoint(): Promise<void>;
}
