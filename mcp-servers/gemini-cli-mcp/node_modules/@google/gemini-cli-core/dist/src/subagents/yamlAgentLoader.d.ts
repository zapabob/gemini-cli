/**
 * @license
 * Copyright 2025 Google LLC
 * SPDX-License-Identifier: Apache-2.0
 */
/**
 * YAMLベースのサブエージェント定義を読み込むローダー
 */
export declare class YamlAgentLoader {
    private agentsDir;
    private registry;
    constructor(agentsDir?: string);
    /**
     * すべてのサブエージェント定義を読み込む
     */
    loadAllAgents(): Promise<void>;
    /**
     * 個別のサブエージェント定義を読み込む
     */
    private loadAgentDefinition;
    /**
     * 新しいサブエージェント定義を作成する
     */
    createAgentDefinition(name: string, specialty: string, description: string): Promise<string>;
    /**
     * サブエージェント定義を削除する
     */
    deleteAgentDefinition(name: string): Promise<boolean>;
}
