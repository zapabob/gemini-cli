/**
 * @license
 * Copyright 2025 Google LLC
 * SPDX-License-Identifier: Apache-2.0
 */
import { SubagentExecutor } from './executor.js';
/**
 * AI自律的オーケストレーター
 * AI側でサブエージェントを自律的に呼び出して作業を分担させる
 */
export class AIAutonomousOrchestrator {
    config;
    executor;
    geminiClient;
    // private activeSessions: Map<string, unknown> = new Map();
    // private checkpointData: Map<string, unknown> = new Map();
    taskHistory = new Map();
    constructor(config) {
        this.config = config;
        this.geminiClient = config.geminiClient;
        this.executor = new SubagentExecutor({
            geminiClient: this.geminiClient,
            maxConcurrent: config.maxSubagents,
            timeout: config.timeout,
            onProgress: this.handleProgress.bind(this)
        });
    }
    /**
     * AI側からの自律的タスク実行
     */
    async executeAIAutonomousTask(task, context, options) {
        const taskId = this.generateTaskId();
        const startTime = Date.now();
        try {
            console.log(`🤖 AI自律的タスク実行開始: ${task}`);
            // 1. AI自律的タスク分析
            const analysis = await this.analyzeAITask(task, context);
            console.log(`📊 AIタスク分析完了: 複雑度 ${analysis.complexity}, 必要専門分野 ${analysis.requiredSpecialties.length}個`);
            // 2. AI自律的サブエージェント選択
            const selection = await this.selectAISubagents(analysis, task);
            console.log(`👥 AIサブエージェント選択完了: ${selection.selectedSubagents.length}個のエージェントを選択`);
            // 3. AI自律的タスク分割と実行
            const results = await this.executeWithAISubagents(selection, task, context, options);
            console.log(`⚡ AI自律的タスク実行完了: ${results.length}個の結果を統合`);
            // 4. AI自律的結果統合
            const integratedResult = await this.integrateAIResults(results, task, analysis);
            console.log(`🔗 AI結果統合完了`);
            // 5. メトリクス計算
            const executionTime = Date.now() - startTime;
            const metrics = this.calculateAIMetrics(results, executionTime, selection.selectedSubagents.length);
            // 6. 実装ログ保存
            await this.saveAIImplementationLog(taskId, task, analysis, selection, results, integratedResult);
            return {
                success: true,
                finalResult: integratedResult,
                mainAgentAnalysis: this.convertToTaskAnalysis(analysis),
                subagentResults: results,
                executionTime,
                collaborationMetrics: metrics,
                taskId
            };
        }
        catch (error) {
            console.error(`❌ AI自律的タスク実行エラー: ${error}`);
            return {
                success: false,
                error: error instanceof Error ? error.message : 'Unknown error',
                taskId,
                executionTime: Date.now() - startTime
            };
        }
    }
    /**
     * AI自律的タスク分析
     */
    async analyzeAITask(task, context) {
        const prompt = `
以下のタスクを分析して、AI自律的実行に必要な情報を抽出してください：

タスク: ${task}
コンテキスト: ${context || 'なし'}

以下の項目を分析してください：
1. タスクの複雑度（1-10）
2. 必要な専門分野
3. 推定サブエージェント数
4. 並列実行可能性
5. 依存関係
6. リスクレベル
7. 推定実行時間
8. サブタスク分割
9. 協調戦略
10. 優先度
11. 自動リサーチ必要性
12. リサーチクエリ（必要な場合）

JSON形式で回答してください。
`;
        try {
            const response = await this.geminiClient.generateText({ prompt });
            const analysis = JSON.parse(response.text);
            return {
                originalTask: task,
                complexity: analysis.complexity || 5,
                requiredSpecialties: analysis.requiredSpecialties || [],
                estimatedSubagents: analysis.estimatedSubagents || 1,
                parallelizable: analysis.parallelizable || false,
                dependencies: analysis.dependencies || [],
                riskLevel: analysis.riskLevel || 'medium',
                estimatedTime: analysis.estimatedTime || 300,
                subtasks: analysis.subtasks || [task],
                coordinationStrategy: analysis.coordinationStrategy || 'adaptive',
                priority: analysis.priority || 'medium',
                autoResearchRequired: analysis.autoResearchRequired || false,
                researchQuery: analysis.researchQuery
            };
        }
        catch (error) {
            console.warn(`⚠️ AIタスク分析エラー、フォールバック分析を使用: ${error}`);
            return this.fallbackAIAnalysis(task);
        }
    }
    /**
     * AI自律的サブエージェント選択
     */
    async selectAISubagents(analysis, task) {
        const availableSubagents = await this.getAvailableSubagents();
        const prompt = `
以下のタスクと分析結果に基づいて、最適なサブエージェントを選択してください：

タスク: ${task}
分析結果: ${JSON.stringify(analysis, null, 2)}
利用可能なサブエージェント: ${JSON.stringify(availableSubagents.map(s => ({ id: s.id, name: s.name, specialty: s.specialty })), null, 2)}

以下の項目を決定してください：
1. 選択するサブエージェント
2. 割り当て戦略（parallel/sequential/hybrid/adaptive）
3. タスク分割
4. 協調計画
5. 実行順序
6. 並列グループ
7. フォールバック計画

JSON形式で回答してください。
`;
        try {
            const response = await this.geminiClient.generateText({ prompt });
            const selection = JSON.parse(response.text);
            const selectedSubagents = availableSubagents.filter(s => selection.selectedSubagents.includes(s.id));
            return {
                selectedSubagents,
                assignmentStrategy: selection.assignmentStrategy || 'adaptive',
                taskBreakdown: new Map(Object.entries(selection.taskBreakdown || {})),
                coordinationPlan: selection.coordinationPlan || '',
                executionOrder: selection.executionOrder || [],
                parallelGroups: selection.parallelGroups || [],
                fallbackPlan: selection.fallbackPlan || ''
            };
        }
        catch (error) {
            console.warn(`⚠️ AIサブエージェント選択エラー、フォールバック選択を使用: ${error}`);
            return this.fallbackAISubagentSelection(analysis, availableSubagents);
        }
    }
    /**
     * AI自律的タスク分割と実行
     */
    async executeWithAISubagents(selection, originalTask, context, options) {
        const results = [];
        try {
            // 適応的実行戦略
            if (selection.assignmentStrategy === 'adaptive') {
                return await this.executeAdaptiveStrategy(selection, originalTask, context, options);
            }
            // 並列実行
            if (selection.assignmentStrategy === 'parallel') {
                const parallelTasks = Array.from(selection.taskBreakdown.entries()).map(([subagentId, subtask]) => ({
                    subagentId,
                    task: subtask,
                    context
                }));
                const results = [];
                for (const task of parallelTasks) {
                    const subagent = selection.selectedSubagents.find(s => s.id === task.subagentId);
                    if (subagent) {
                        const result = await this.executor.executeTask(subagent, {
                            id: task.subagentId,
                            task: task.task,
                            context: task.context,
                            priority: 'medium'
                        });
                        results.push(result);
                    }
                }
                return results;
            }
            // 順次実行
            if (selection.assignmentStrategy === 'sequential') {
                for (const [subagentId, subtask] of selection.taskBreakdown) {
                    const subagent = selection.selectedSubagents.find(s => s.id === subagentId);
                    if (subagent) {
                        const result = await this.executor.executeTask(subagent, {
                            id: subagentId,
                            task: subtask,
                            context,
                            priority: 'medium'
                        });
                        results.push(result);
                    }
                }
                return results;
            }
            // ハイブリッド実行
            if (selection.assignmentStrategy === 'hybrid') {
                return await this.executeHybridStrategy(selection, originalTask, context, options);
            }
            // デフォルトは適応的実行
            return await this.executeAdaptiveStrategy(selection, originalTask, context, options);
        }
        catch (error) {
            console.error(`❌ AI自律的タスク実行エラー: ${error}`);
            throw error;
        }
    }
    /**
     * 適応的実行戦略
     */
    async executeAdaptiveStrategy(selection, originalTask, context, options) {
        const results = [];
        // 動的に実行戦略を決定
        const strategy = await this.determineAdaptiveStrategy(selection, originalTask);
        if (strategy === 'parallel') {
            const parallelTasks = Array.from(selection.taskBreakdown.entries()).map(([subagentId, subtask]) => ({
                subagentId,
                task: subtask,
                context
            }));
            const results = [];
            for (const task of parallelTasks) {
                const subagent = selection.selectedSubagents.find(s => s.id === task.subagentId);
                if (subagent) {
                    const result = await this.executor.executeTask(subagent, {
                        id: task.subagentId,
                        task: task.task,
                        context: task.context,
                        priority: 'medium'
                    });
                    results.push(result);
                }
            }
            return results;
        }
        if (strategy === 'sequential') {
            for (const [subagentId, subtask] of selection.taskBreakdown) {
                const subagent = selection.selectedSubagents.find(s => s.id === subagentId);
                if (subagent) {
                    const result = await this.executor.executeTask(subagent, {
                        id: subagentId,
                        task: subtask,
                        context,
                        priority: 'medium'
                    });
                    results.push(result);
                }
            }
            return results;
        }
        // デフォルトはハイブリッド
        return await this.executeHybridStrategy(selection, originalTask, context, options);
    }
    /**
     * ハイブリッド実行戦略
     */
    async executeHybridStrategy(selection, originalTask, context, options) {
        const results = [];
        // 並列グループごとに実行
        for (const group of selection.parallelGroups) {
            const groupSubagents = selection.selectedSubagents.filter(s => group.includes(s.id));
            const groupTasks = group.map(subagentId => {
                const subtask = selection.taskBreakdown.get(subagentId);
                return { subagentId, task: subtask || originalTask, context };
            });
            const groupResults = [];
            for (const task of groupTasks) {
                const subagent = groupSubagents.find(s => s.id === task.subagentId);
                if (subagent) {
                    const result = await this.executor.executeTask(subagent, {
                        id: task.subagentId,
                        task: task.task,
                        context: task.context,
                        priority: 'medium'
                    });
                    groupResults.push(result);
                }
            }
            results.push(...groupResults);
        }
        return results;
    }
    /**
     * 適応的戦略決定
     */
    async determineAdaptiveStrategy(selection, originalTask) {
        const prompt = `
以下の条件に基づいて、最適な実行戦略を決定してください：

タスク: ${originalTask}
サブエージェント数: ${selection.selectedSubagents.length}
タスク分割: ${JSON.stringify(Array.from(selection.taskBreakdown.entries()))}
並列グループ: ${JSON.stringify(selection.parallelGroups)}

実行戦略の選択基準：
- parallel: 独立したタスクで並列実行可能
- sequential: 依存関係があるタスク
- hybrid: 部分的に並列実行可能

戦略を選択してください（parallel/sequential/hybrid）
`;
        try {
            const response = await this.geminiClient.generateText({ prompt });
            const strategy = response.text.trim().toLowerCase();
            if (['parallel', 'sequential', 'hybrid'].includes(strategy)) {
                return strategy;
            }
            return 'hybrid'; // デフォルト
        }
        catch (error) {
            console.warn(`⚠️ 適応的戦略決定エラー、ハイブリッド戦略を使用: ${error}`);
            return 'hybrid';
        }
    }
    /**
     * AI自律的結果統合
     */
    async integrateAIResults(results, originalTask, analysis) {
        const prompt = `
以下の結果を統合して、元のタスクに対する完全な回答を作成してください：

元のタスク: ${originalTask}
分析結果: ${JSON.stringify(analysis, null, 2)}
サブエージェント結果: ${JSON.stringify(results, null, 2)}

統合の際は以下を考慮してください：
1. 結果の一貫性
2. 重複の除去
3. 矛盾の解決
4. 優先度の考慮
5. 元のタスクとの整合性

統合された結果を返してください。
`;
        try {
            const response = await this.geminiClient.generateText({ prompt });
            return {
                finalResult: response.text,
                qualityScore: 0.8,
                confidenceLevel: 0.7,
                recommendations: []
            };
        }
        catch (error) {
            console.error(`❌ AI結果統合エラー: ${error}`);
            throw error;
        }
    }
    /**
     * AIメトリクス計算
     */
    calculateAIMetrics(results, executionTime, subagentsUsed) {
        const successCount = results.filter(r => r.status === 'success').length;
        const totalCount = results.length;
        // const successRate = totalCount > 0 ? (successCount / totalCount) * 100 : 0;
        return {
            totalSteps: totalCount,
            successfulSteps: successCount,
            totalTokensUsed: results.reduce((total, r) => total + (r.tokensUsed || 0), 0),
            averageResponseTime: executionTime / totalCount,
            subagentsUsed: subagentsUsed,
            errorRate: totalCount > 0 ? ((totalCount - successCount) / totalCount) * 100 : 0
        };
    }
    /**
     * 協調効率計算
     */
    calculateCoordinationEfficiency(results) {
        if (results.length === 0)
            return 0;
        const successfulResults = results.filter(r => r.status === 'success');
        const coordinationScore = successfulResults.reduce((score, result) => {
            // 結果の品質と協調度を評価
            const qualityScore = result.qualityScore || 0.5;
            const confidenceScore = result.confidenceLevel || 0.5;
            return score + (qualityScore + confidenceScore) / 2;
        }, 0);
        return coordinationScore / results.length;
    }
    /**
     * リソース利用率計算
     */
    calculateResourceUtilization(results, executionTime) {
        if (results.length === 0)
            return 0;
        const totalWorkTime = results.reduce((total, result) => {
            return total + (result.executionTime || 0);
        }, 0);
        const maxPossibleTime = results.length * executionTime;
        return maxPossibleTime > 0 ? (totalWorkTime / maxPossibleTime) * 100 : 0;
    }
    /**
     * 利用可能なサブエージェント取得
     */
    async getAvailableSubagents() {
        // 実際のサブエージェント設定から取得
        return [
            {
                id: 'code_reviewer',
                name: 'コードレビュアー',
                description: 'コードレビュー専門家',
                specialty: 'code_review',
                prompt: 'あなたはコードレビュー専門家です。コードの品質、セキュリティ、パフォーマンスを評価してください。',
                maxTokens: 4000,
                temperature: 0.7,
                status: 'idle',
                createdAt: new Date().toISOString(),
                taskHistory: [],
                customTools: [],
                isActive: true
            },
            {
                id: 'debugger',
                name: 'デバッガー',
                description: 'デバッグ専門家',
                specialty: 'debugging',
                prompt: 'あなたはデバッグ専門家です。エラーの原因を特定し、解決策を提案してください。',
                maxTokens: 4000,
                temperature: 0.7,
                status: 'idle',
                createdAt: new Date().toISOString(),
                taskHistory: [],
                customTools: [],
                isActive: true
            },
            {
                id: 'data_analyst',
                name: 'データアナリスト',
                description: 'データ分析専門家',
                specialty: 'data_analysis',
                prompt: 'あなたはデータ分析専門家です。データの可視化と統計解析を行ってください。',
                maxTokens: 4000,
                temperature: 0.7,
                status: 'idle',
                createdAt: new Date().toISOString(),
                taskHistory: [],
                customTools: [],
                isActive: true
            },
            {
                id: 'security_auditor',
                name: 'セキュリティ監査官',
                description: 'セキュリティ監査専門家',
                specialty: 'security_audit',
                prompt: 'あなたはセキュリティ監査専門家です。セキュリティ脆弱性を特定し、対策を提案してください。',
                maxTokens: 4000,
                temperature: 0.7,
                status: 'idle',
                createdAt: new Date().toISOString(),
                taskHistory: [],
                customTools: [],
                isActive: true
            },
            {
                id: 'performance_optimizer',
                name: 'パフォーマンス最適化者',
                description: 'パフォーマンス最適化専門家',
                specialty: 'performance_optimization',
                prompt: 'あなたはパフォーマンス最適化専門家です。コードの最適化とパフォーマンス向上を提案してください。',
                maxTokens: 4000,
                temperature: 0.7,
                status: 'idle',
                createdAt: new Date().toISOString(),
                taskHistory: [],
                customTools: [],
                isActive: true
            },
            {
                id: 'documentation_writer',
                name: 'ドキュメント作成者',
                description: 'ドキュメント作成専門家',
                specialty: 'documentation',
                prompt: 'あなたはドキュメント作成専門家です。技術文書とユーザーガイドを作成してください。',
                maxTokens: 4000,
                temperature: 0.7,
                status: 'idle',
                createdAt: new Date().toISOString(),
                taskHistory: [],
                customTools: [],
                isActive: true
            },
            {
                id: 'tester',
                name: 'テスター',
                description: 'テスト専門家',
                specialty: 'testing',
                prompt: 'あなたはテスト専門家です。テスト戦略と品質保証を提案してください。',
                maxTokens: 4000,
                temperature: 0.7,
                status: 'idle',
                createdAt: new Date().toISOString(),
                taskHistory: [],
                customTools: [],
                isActive: true
            },
            {
                id: 'architect',
                name: 'アーキテクト',
                description: 'アーキテクチャ設計専門家',
                specialty: 'architecture_design',
                prompt: 'あなたはアーキテクチャ設計専門家です。スケーラブルで保守性の高いシステム設計を提案してください。',
                maxTokens: 4000,
                temperature: 0.7,
                status: 'idle',
                createdAt: new Date().toISOString(),
                taskHistory: [],
                customTools: [],
                isActive: true
            }
        ];
    }
    /**
     * フォールバックAI分析
     */
    fallbackAIAnalysis(task) {
        return {
            originalTask: task,
            complexity: 5,
            requiredSpecialties: ['custom'],
            estimatedSubagents: 1,
            parallelizable: false,
            dependencies: [],
            riskLevel: 'medium',
            estimatedTime: 300,
            subtasks: [task],
            coordinationStrategy: 'sequential',
            priority: 'medium',
            autoResearchRequired: false
        };
    }
    /**
     * フォールバックAIサブエージェント選択
     */
    async fallbackAISubagentSelection(analysis, availableSubagents) {
        const selectedSubagents = availableSubagents.slice(0, Math.min(analysis.estimatedSubagents, availableSubagents.length));
        return {
            selectedSubagents,
            assignmentStrategy: 'sequential',
            taskBreakdown: new Map([[selectedSubagents[0]?.id || 'default', analysis.originalTask]]),
            coordinationPlan: 'シーケンシャル実行',
            executionOrder: [selectedSubagents[0]?.id || 'default'],
            parallelGroups: [],
            fallbackPlan: '単一サブエージェントで実行'
        };
    }
    /**
     * 進捗ハンドラー
     */
    handleProgress(message, type) {
        console.log(`🤖 AI自律的オーケストレーター: ${message}`);
    }
    /**
     * タスクID生成
     */
    generateTaskId() {
        return `ai_autonomous_${Date.now()}_${Math.random().toString(36).substr(2, 9)}`;
    }
    /**
     * AI実装ログ保存
     */
    async saveAIImplementationLog(taskId, task, analysis, selection, results, integratedResult) {
        const logData = {
            taskId,
            timestamp: new Date().toISOString(),
            task,
            analysis,
            selection,
            results,
            integratedResult
        };
        // 実装ログを保存
        this.taskHistory.set(taskId, analysis);
        console.log(`📝 AI実装ログ保存完了: ${taskId}`);
    }
    /**
     * TaskAnalysis変換
     */
    convertToTaskAnalysis(analysis) {
        return {
            originalTask: analysis.originalTask,
            requiredSpecialties: analysis.requiredSpecialties,
            complexity: analysis.complexity,
            estimatedTime: analysis.estimatedTime,
            requiredSteps: analysis.subtasks.length,
            riskFactors: [analysis.riskLevel],
            successCriteria: ['タスク完了', '品質保証', '時間内完了']
        };
    }
}
//# sourceMappingURL=aiAutonomousOrchestrator.js.map