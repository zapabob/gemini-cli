/**
 * @license
 * Copyright 2025 Google LLC
 * SPDX-License-Identifier: Apache-2.0
 */
import { Subagent } from '../config/subagents.js';
import { SubagentResult } from './executor.js';
/**
 * 監督者エージェントの役割定義
 */
export interface SupervisorRole {
    id: string;
    name: string;
    description: string;
    responsibilities: string[];
    decisionMakingAuthority: 'high' | 'medium' | 'low';
    coordinationStyle: 'autocratic' | 'democratic' | 'laissez-faire';
}
/**
 * サブエージェントタスクの依存関係
 */
export interface TaskDependency {
    taskId: string;
    dependsOn: string[];
    requiredOutputs: string[];
    blocking: boolean;
}
/**
 * 監督者エージェントの設定
 */
export interface SupervisorConfig {
    role: SupervisorRole;
    maxSubagents: number;
    coordinationStrategy: 'sequential' | 'parallel' | 'hybrid';
    decisionThreshold: number;
    progressReporting: boolean;
    errorHandling: 'strict' | 'flexible' | 'adaptive';
}
/**
 * 監督者エージェントの実行結果
 */
export interface SupervisorResult {
    success: boolean;
    finalOutput: string;
    subagentResults: SubagentResult[];
    coordinationLog: string[];
    executionTime: number;
    decisions: DecisionLog[];
    errors: string[];
}
/**
 * 決定ログ
 */
export interface DecisionLog {
    timestamp: Date;
    decision: string;
    reasoning: string;
    subagentId?: string;
    impact: 'high' | 'medium' | 'low';
}
/**
 * 監督者エージェント
 * メインエージェントが監督者として機能し、サブエージェントが専門的なタスクを分担
 */
export declare class SupervisorAgent {
    private config;
    private executor;
    private geminiClient;
    private decisionLog;
    private coordinationLog;
    constructor(config: SupervisorConfig);
    /**
     * 監督者としてのタスク実行
     * 例: Aという実装の監督
     */
    superviseImplementation(implementationGoal: string, subagents: Subagent[], context?: string): Promise<SupervisorResult>;
    /**
     * 実装目標の分析
     */
    private analyzeImplementationGoal;
    /**
     * サブエージェントの役割割り当て
     */
    private assignSubagentRoles;
    /**
     * 並列実行の調整
     */
    private coordinateParallelExecution;
    /**
     * 結果の統合と最終決定
     */
    private integrateResults;
    /**
     * 進捗の処理
     */
    private handleProgress;
    /**
     * 調整ログの記録
     */
    private logCoordination;
    /**
     * 決定ログの記録
     */
    private logDecision;
    /**
     * サブエージェントの取得（簡略化）
     */
    private getSubagentsByIds;
}
