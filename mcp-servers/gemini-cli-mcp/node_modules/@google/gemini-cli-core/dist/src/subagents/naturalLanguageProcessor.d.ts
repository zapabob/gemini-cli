/**
 * @license
 * Copyright 2025 Google LLC
 * SPDX-License-Identifier: Apache-2.0
 */
import { GeminiClient } from './geminiClient.js';
import { Subagent, SubagentSpecialty } from '../config/subagents.js';
import { CollaborativeTaskOptions, CollaborativeTaskResult } from './types.js';
import { Config } from '../config/config.js';
/**
 * 自然言語プロンプト解析結果
 */
export interface NaturalLanguageAnalysis {
    originalPrompt: string;
    mainTask: string;
    subtasks: string[];
    requiredSpecialties: SubagentSpecialty[];
    parallelizable: boolean;
    estimatedComplexity: number;
    requiresResearch: boolean;
    researchQuery?: string;
    executionStrategy: 'parallel' | 'sequential' | 'hybrid';
    priority: 'low' | 'medium' | 'high';
    estimatedTime: number;
    dependencies: string[];
}
/**
 * タスク分割結果
 */
export interface TaskBreakdown {
    mainTask: string;
    subtasks: Map<string, string>;
    subagentAssignments: Map<string, Subagent>;
    coordinationPlan: string;
    executionOrder: string[];
    parallelGroups: string[][];
}
/**
 * 自然言語プロンプトプロセッサー設定
 */
export interface NaturalLanguageProcessorConfig {
    geminiClient: GeminiClient;
    config: Config;
    enableAutoResearch: boolean;
    enableParallelExecution: boolean;
    maxConcurrentTasks: number;
    researchOutputPath: string;
    enableCheckpointing: boolean;
    autoSaveInterval: number;
}
/**
 * 自然言語プロンプトプロセッサー
 * 自然言語のプロンプトを解析して並列作業を自律的に分担する
 */
export declare class NaturalLanguageProcessor {
    private config;
    private orchestrator;
    private deepResearchTool;
    private geminiClient;
    private activeTasks;
    private checkpointData;
    constructor(config: NaturalLanguageProcessorConfig);
    /**
     * 自然言語プロンプトを処理して並列作業を実行
     */
    processNaturalLanguagePrompt(prompt: string, context?: string, options?: CollaborativeTaskOptions): Promise<CollaborativeTaskResult>;
    /**
     * 自然言語プロンプトを解析
     */
    private analyzeNaturalLanguagePrompt;
    /**
     * フォールバック解析（AI解析が失敗した場合）
     */
    private fallbackAnalysis;
    /**
     * リサーチ実行
     */
    private executeResearch;
    /**
     * リサーチ結果の保存
     */
    private saveResearchResult;
    /**
     * タスク分割とサブエージェント割り当て
     */
    private breakdownTasks;
    /**
     * 利用可能なサブエージェントの取得
     */
    private getAvailableSubagents;
    /**
     * 最適なサブエージェントの選択
     */
    private findBestSubagent;
    /**
     * 協調計画の生成
     */
    private generateCoordinationPlan;
    /**
     * 並列タスク実行
     */
    private executeParallelTasks;
    /**
     * 結果統合
     */
    private integrateResults;
    /**
     * 実装ログの保存
     */
    private saveImplementationLog;
    /**
     * タスクID生成
     */
    private generateTaskId;
}
