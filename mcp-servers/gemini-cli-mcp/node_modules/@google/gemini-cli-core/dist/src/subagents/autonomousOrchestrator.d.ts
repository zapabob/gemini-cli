/**
 * @license
 * Copyright 2025 Google LLC
 * SPDX-License-Identifier: Apache-2.0
 */
import { Subagent, SubagentSpecialty } from '../config/subagents.js';
import { GeminiClient } from './geminiClient.js';
import { CollaborativeTaskOptions, CollaborativeTaskResult } from './types.js';
/**
 * 自律的オーケストレーター設定
 */
export interface AutonomousOrchestratorConfig {
    geminiClient: GeminiClient;
    maxSubagents: number;
    enableAutoAnalysis: boolean;
    enableRealTimeCoordination: boolean;
    decisionThreshold: number;
    timeout: number;
    enableCheckpointing: boolean;
    checkpointInterval: number;
}
/**
 * タスク分析結果
 */
export interface TaskAnalysisResult {
    complexity: number;
    requiredSpecialties: SubagentSpecialty[];
    estimatedSubagents: number;
    parallelizable: boolean;
    dependencies: string[];
    riskLevel: 'low' | 'medium' | 'high';
    estimatedTime: number;
}
/**
 * サブエージェント選択結果
 */
export interface SubagentSelectionResult {
    selectedSubagents: Subagent[];
    assignmentStrategy: 'parallel' | 'sequential' | 'hybrid';
    taskBreakdown: Map<string, string>;
    coordinationPlan: string;
}
/**
 * 自律的オーケストレーター
 * メインエージェントが自律的にサブエージェントを選択・呼び出し・協調させる
 */
export declare class AutonomousOrchestrator {
    private config;
    private executor;
    private geminiClient;
    private activeSessions;
    private checkpointData;
    constructor(config: AutonomousOrchestratorConfig);
    /**
     * メインエージェントからの自律的タスク実行
     */
    executeAutonomousTask(task: string, context?: string, _options?: CollaborativeTaskOptions): Promise<CollaborativeTaskResult>;
    /**
     * タスクの自動分析
     */
    private analyzeTask;
    /**
     * サブエージェントの自動選択
     */
    private selectSubagents;
    /**
     * サブエージェントとの実行
     */
    private executeWithSubagents;
    /**
     * 結果の統合
     */
    private integrateResults;
    /**
     * 利用可能なサブエージェントを取得
     */
    private getAvailableSubagents;
    /**
     * タスクの分割
     */
    private breakdownTask;
    /**
     * サブタスク生成
     */
    private generateSubtask;
    /**
     * 協調計画の生成
     */
    private generateCoordinationPlan;
    /**
     * メトリクス計算
     */
    private calculateMetrics;
    /**
     * 型変換ヘルパー
     */
    private convertToTaskAnalysis;
    /**
     * 進捗ハンドラー
     */
    private handleProgress;
    /**
     * タスクID生成
     */
    private generateTaskId;
    /**
     * チェックポイント保存
     */
    private saveCheckpoint;
    /**
     * チェックポイント復元
     */
    private restoreCheckpoint;
}
