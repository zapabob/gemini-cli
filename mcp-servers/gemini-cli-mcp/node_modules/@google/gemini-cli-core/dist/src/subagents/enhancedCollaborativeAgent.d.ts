/**
 * @license
 * Copyright 2025 Google LLC
 * SPDX-License-Identifier: Apache-2.0
 */
import { Subagent } from '../config/subagents.js';
import { GeminiClient } from './geminiClient.js';
import { RealTimeCollaborationOptions, RealTimeCollaborationResult, RealTimeSessionConfig } from './types.js';
/**
 * 強化版協調エージェントシステム
 * リアルタイム通信システムを統合したメインエージェントとサブエージェントの協調作業
 */
export declare class EnhancedCollaborativeAgentSystem {
    private mainAgent;
    private subagents;
    private executor;
    private geminiClient;
    private realTimeSystem;
    private sessionConfig;
    private activeSessions;
    private performanceMonitor;
    constructor(geminiClient: GeminiClient, sessionConfig: RealTimeSessionConfig);
    /**
     * リアルタイム通信ハンドラーの設定
     */
    private setupRealTimeHandlers;
    /**
     * サブエージェントの登録
     */
    registerSubagent(subagent: Subagent): void;
    /**
     * リアルタイム協調タスク実行
     */
    executeRealTimeCollaboration(task: string, context?: string, options?: RealTimeCollaborationOptions): Promise<RealTimeCollaborationResult>;
    /**
     * リアルタイムセッション開始
     */
    private startRealTimeSession;
    /**
     * リアルタイムセッション終了
     */
    private endRealTimeSession;
    /**
     * タスク割り当てハンドラー
     */
    private handleTaskAssignment;
    /**
     * タスク進捗ハンドラー
     */
    private handleTaskProgress;
    /**
     * タスク完了ハンドラー
     */
    private handleTaskCompletion;
    /**
     * 協調要求ハンドラー
     */
    private handleCoordinationRequest;
    /**
     * メインエージェント指示ハンドラー
     */
    private handleMainAgentDirective;
    /**
     * 指示の実行
     */
    private executeDirective;
    /**
     * サブエージェントレポートハンドラー
     */
    private handleSubagentReport;
    /**
     * パフォーマンスメトリクスハンドラー
     */
    private handlePerformanceMetrics;
    /**
     * システム状態の取得
     */
    getSystemStatus(): unknown;
    /**
     * システムの停止
     */
    shutdown(): Promise<void>;
    /**
     * ユーティリティ関数
     */
    private generateSessionId;
    private generateTaskId;
    private generateMessageId;
}
