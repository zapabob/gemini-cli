/**
 * @license
 * Copyright 2025 Google LLC
 * SPDX-License-Identifier: Apache-2.0
 */
/**
 * チェックポイントデータ構造
 */
export interface CheckpointData {
    sessionId: string;
    taskId: string;
    timestamp: number;
    data: unknown;
    metadata: {
        version: string;
        checksum: string;
        backupCount: number;
    };
}
/**
 * セッション状態
 */
export interface SessionState {
    sessionId: string;
    status: 'active' | 'paused' | 'completed' | 'failed';
    startTime: number;
    lastActivity: number;
    participants: string[];
    taskProgress: number;
    data: unknown;
}
/**
 * チェックポイントマネージャー設定
 */
export interface CheckpointManagerConfig {
    checkpointDir: string;
    maxBackups: number;
    autoSaveInterval: number;
    enableCompression: boolean;
    enableEncryption: boolean;
    encryptionKey?: string;
    recoveryMode: 'auto' | 'manual' | 'disabled';
}
/**
 * リカバリー情報
 */
export interface RecoveryInfo {
    sessionId: string;
    lastCheckpoint: number;
    recoveryData: unknown;
    canRecover: boolean;
    estimatedRecoveryTime: number;
}
/**
 * 電源断保護機能付きチェックポイントマネージャー
 * 自動チェックポイント保存、緊急保存、バックアップローテーション、セッション管理を提供
 */
export declare class CheckpointManager {
    private config;
    private activeSessions;
    private checkpointData;
    private autoSaveTimer?;
    private isShuttingDown;
    constructor(config: CheckpointManagerConfig);
    /**
     * シグナルハンドラーの設定
     */
    private setupSignalHandlers;
    /**
     * チェックポイントディレクトリの初期化
     */
    private initializeCheckpointDirectory;
    /**
     * セッション開始
     */
    startSession(sessionId: string, initialData: unknown): Promise<void>;
    /**
     * セッション更新
     */
    updateSession(sessionId: string, updates: Partial<SessionState>): Promise<void>;
    /**
     * セッション終了
     */
    endSession(sessionId: string, finalData: unknown): Promise<void>;
    /**
     * チェックポイント保存
     */
    saveCheckpoint(sessionId: string, data: unknown, isFinal?: boolean): Promise<void>;
    /**
     * ファイルへの保存
     */
    private saveToFile;
    /**
     * チェックポイント復元
     */
    restoreCheckpoint(taskId: string): Promise<unknown | null>;
    /**
     * ファイルからの読み込み
     */
    private loadFromFile;
    /**
     * リカバリー情報の取得
     */
    getRecoveryInfo(sessionId: string): Promise<RecoveryInfo | null>;
    /**
     * セッション復旧
     */
    recoverSession(sessionId: string): Promise<boolean>;
    /**
     * 緊急保存
     */
    emergencySave(): Promise<void>;
    /**
     * 自動保存開始
     */
    private startAutoSave;
    /**
     * チェックポイント保存判定
     */
    private shouldSaveCheckpoint;
    /**
     * バックアップローテーション
     */
    private rotateBackups;
    /**
     * セッションチェックポイント取得
     */
    private getSessionCheckpoints;
    /**
     * チェックポイント削除
     */
    private deleteCheckpoint;
    /**
     * バックアップ数取得
     */
    private getBackupCount;
    /**
     * チェックサム計算
     */
    private calculateChecksum;
    /**
     * 復旧時間推定
     */
    private estimateRecoveryTime;
    /**
     * データ圧縮（簡易版）
     */
    private compressData;
    /**
     * データ解凍（簡易版）
     */
    private decompressData;
    /**
     * データ暗号化（簡易版）
     */
    private encryptData;
    /**
     * データ復号化（簡易版）
     */
    private decryptData;
    /**
     * クリーンアップ
     */
    cleanup(): Promise<void>;
}
