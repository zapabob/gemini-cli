/**
 * @license
 * Copyright 2025 Google LLC
 * SPDX-License-Identifier: Apache-2.0
 */
/**
 * Flicker frame data
 */
export interface FlickerFrame {
    timestamp: number;
    duration: number;
    severity: 'low' | 'medium' | 'high';
    component?: string;
}
/**
 * Record a flicker frame
 */
export declare function recordFlickerFrame(frame: FlickerFrame): void;
/**
 * Flicker detector class
 */
export declare class FlickerDetector {
    private frames;
    private readonly maxFrames;
    /**
     * Add a flicker frame
     */
    addFrame(frame: FlickerFrame): void;
    /**
     * Get flicker statistics
     */
    getStats(): {
        totalFrames: number;
        averageDuration: number;
        severityCounts: Record<string, number>;
    };
    /**
     * Clear all frames
     */
    clear(): void;
}
