/**
 * @license
 * Copyright 2025 Google LLC
 * SPDX-License-Identifier: Apache-2.0
 */
/**
 * Session file prefix constant
 */
export declare const SESSION_FILE_PREFIX = "session_";
/**
 * Session utilities for managing session files
 */
export declare class SessionUtils {
    /**
     * Generate a session file name with timestamp
     */
    static generateSessionFileName(): string;
    /**
     * Check if a file is a session file
     */
    static isSessionFile(filename: string): boolean;
    /**
     * Extract timestamp from session file name
     */
    static extractTimestampFromSessionFile(filename: string): Date | null;
}
