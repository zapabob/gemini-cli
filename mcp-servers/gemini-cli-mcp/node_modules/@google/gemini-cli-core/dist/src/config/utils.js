/**
 * @license
 * Copyright 2025 Google LLC
 * SPDX-License-Identifier: Apache-2.0
 */
import { readFile, writeFile } from 'node:fs/promises';
import { join } from 'node:path';
const CONFIG_DIR = '.gemini';
export async function readConfigFile(fileName, schema, defaultValue) {
    const configPath = join(process.cwd(), CONFIG_DIR, fileName);
    try {
        const content = await readFile(configPath, 'utf-8');
        const parsedContent = JSON.parse(content);
        return schema.parse(parsedContent);
    }
    catch (error) {
        if (error.code === 'ENOENT') {
            return defaultValue;
        }
        throw error;
    }
}
export async function writeConfigFile(fileName, config) {
    const configPath = join(process.cwd(), CONFIG_DIR, fileName);
    await writeFile(configPath, JSON.stringify(config, null, 2), 'utf-8');
}
//# sourceMappingURL=utils.js.map